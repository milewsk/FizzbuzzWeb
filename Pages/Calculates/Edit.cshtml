@page
@model FizzbuzzWeb.Pages.Calculates.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Calculate</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Calculate.Id" />
            <div class="form-group">
                <label asp-for="Calculate.Number" class="control-label"></label>
                <input asp-for="Calculate.Number" class="form-control" />
                <span asp-validation-for="Calculate.Number" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Calculate.Result" class="control-label"></label>
                <input asp-for="Calculate.Result" class="form-control" />
                <span asp-validation-for="Calculate.Result" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Calculate.Time" class="control-label"></label>
                <input asp-for="Calculate.Time" class="form-control" />
                <span asp-validation-for="Calculate.Time" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
